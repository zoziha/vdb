module global

    implicit none
    !> Newline character
    character(*), parameter :: NL = new_line("\n")
    !> launch.json directory
    character(*), parameter :: dir = ".vscode/launch.json"

end module global